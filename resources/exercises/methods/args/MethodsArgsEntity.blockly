# BEGIN SOLUTION 
def move(nbPas, goForward):
    if goForward:
        for i in range(nbPas):
            forward()
    else:
        for i in range(nbPas):
            backward()
# END SOLUTION

move(getY(), entity.getDirection() == Direction.NORTH) 
